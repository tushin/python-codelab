# 코드랩 사이트 제작 작업지시서 (현행 구조 기준)

## 1) 목적
이 문서는 현재 `src/` 구조를 기준으로 코드랩 사이트를 일관되게 운영하기 위한 표준 규칙이다.

핵심 목표:
1. `src/index.html` 단일 파일에서 허브와 코드랩 상세를 함께 렌더링한다.
2. 각 코드랩은 `src/<slug>/` 디렉토리 단위로 관리한다.
3. 코드랩별 비공통 정보는 `INFO.md`에 분리한다.

---

## 2) 현재 적용된 구조

```text
project-root/
  CODELAB_WORK_INSTRUCTION.md
  DESIGN_GUIDE/
  src/
    index.html                    # 허브 + 코드랩 상세 통합 페이지 (CSS/JS 인라인 포함)
    snake/
      INFO.md
      CODELAB.md
      hero.svg
    memory-game/
      INFO.md
      CODELAB.md
      hero.svg
    dino-run/
      INFO.md
      CODELAB.md
      hero.svg
```

---

## 3) 디렉토리/파일 규칙 (중요)

### 필수 규칙
- 웹 렌더링 대상은 모두 `src/` 하위에 둔다.
- `src/index.html`은 사이트의 단일 진입점이다.
- `shared/`, `codelabs/` 같은 중간 디렉토리를 다시 만들지 않는다.
- 코드랩별 `index.html`을 만들지 않는다.

### 코드랩 단위 필수 파일
각 코드랩 디렉토리(`src/<slug>/`)는 반드시 아래 3개를 가진다.
- `INFO.md`
- `CODELAB.md`
- `hero.svg`

---

## 4) INFO.md 규격

`INFO.md`는 `key: value` 형식의 단순 메타 파일이다.
현재 렌더러가 읽는 키:

- `slug`: URL 파라미터와 일치해야 함 (`?lab=<slug>`)
- `eyebrow`: 상세 상단 보조 라벨
- `title`: 코드랩 제목
- `subtitle`: 상세 히어로 설명
- `hero_alt`: 히어로 이미지 대체 텍스트
- `hub_chip`: 허브 카드 칩 텍스트
- `hub_summary`: 허브 카드 설명
- `hub_meta`: 현재 허브에는 출력하지 않음(보관용)

예시:

```md
slug: snake
eyebrow: Snake Lab Curriculum
title: 지렁이 게임(Snake) 코드랩
subtitle: 입력, 이동 계산, 충돌 판정, 화면 렌더링을 단계별로 완성합니다.
hero_alt: 격자 위를 움직이는 뱀 코드랩 일러스트
hub_chip: Python · Pygame
hub_summary: 입력, 이동, 충돌, 점수를 순서대로 구현합니다.
hub_meta: class 1 ~ class 4
```

---

## 5) 신규 코드랩 추가 절차 (표준)

1. `src/<slug>/` 디렉토리를 생성한다.
2. `INFO.md`, `CODELAB.md`, `hero.svg`를 작성/배치한다.
3. `src/index.html` 인라인 스크립트의 `LAB_SLUGS` 배열에 `<slug>`를 추가한다.
4. 브라우저에서 `http://127.0.0.1:3000/?lab=<slug>`와 허브 카드 노출을 확인한다.

주의:
- 허브 카드는 `LAB_SLUGS`와 `INFO.md`를 기반으로 자동 렌더링된다.
- 카드 HTML을 별도로 하드코딩하지 않는다.

---

## 6) 수정 작업 기준

### 코드랩 내용 수정
- `src/<slug>/CODELAB.md`: 본문 학습 콘텐츠 수정
- `src/<slug>/INFO.md`: 제목/설명/카드 문구/접근성 텍스트 수정
- `src/<slug>/hero.svg`: 대표 이미지 수정

### 공통 UI/로직 수정
- `src/index.html` 내부 인라인 `<style>`: 레이아웃/디자인 변경
- `src/index.html` 내부 인라인 `<script>`: 렌더링/목차/파싱/상호작용 변경

---

## 7) 허브/상세 동작 규칙

- 허브 화면: `src/index.html` (쿼리 파라미터 없음)
- 상세 화면: `src/index.html?lab=<slug>`
- 허브 그리드: 최대 2열, 모바일(680px 이하) 1열
- 허브 복귀: 상세 우측 상단의 `허브로 돌아가기`

---

## 8) 설명 작성 규칙 (중1·비전공 기준)

### 금지
- 추상 문구만 단독으로 쓰기
  - 예: "핵심 로직을 처리한다"
- 다른 케이스에도 적용될 수 있는 표현
  - 예: "값을 계산/초기화해 이후 로직에서 사용한다."
- 코드를 설명하지 않는 내용
  - 예: "현재 단계 핵심 동작에 필요한 코드다."
- 당연한 말 반복하기
  - 예: "현재 단계에서 바로 실행되는 코드다."
  - 예: "이 줄이 실행되어야 정상 동작한다."

### 권장
각 설명에 아래 4가지를 함께 포함한다.
1. 이 코드가 어떤 값을 바꾸는지
2. 그 값이 언제/어디서 사용되는지
3. 화면/동작 결과가 어떻게 바뀌는지
4. 일반적인 코드의 기능이 아닌 이 코드만의 기능

### 핵심코드 설명 작성 규칙 (필수)
- 적용 대상: 모든 `코드 블럭의 핵심코드 및 처음 배우는 표현 설명` 항목
- 각 항목은 "무엇을 한다"에서 끝내지 않고, "그래서 다음에 무엇이 가능해지는지"까지 설명한다. 단 "이후 입력 분기, 상태 업데이트, 렌더링에서 참조되어 실제 동작이 결정된다." 와 같은 당연한 설명은 하지 않는다.
- 함수/메서드 호출 설명은 반드시 아래를 포함한다.
  - 호출 시점에 실제로 작동하는 대상(예: 입력, 렌더링, 시간, 충돌, 사운드)
  - 호출 결과가 이후 어떤 분기/업데이트/화면 반영으로 이어지는지
- 대입문/조건문/반복문 설명도 반드시 해당 줄의 역할을 코드 흐름 기준으로 적는다.
  - 대입문: 어떤 상태값을 확정하고, 이후 어느 계산에서 참조되는지
  - 조건문: 어떤 규칙을 차단/허용하는지
  - 반복문: 어떤 목록/프레임 처리를 빠짐없이 적용하는지

### 초기화 코드 예시 기준 (`pygame.init()`)
- 초기화 코드는 "준비한다" 수준에서 끝내지 말고 준비 대상까지 적는다.
- `pygame.init()` 설명 기준:
  - "키보드 입력, 화면 출력, 사운드 같은 pygame 하위 모듈을 한 번에 초기화해 하드웨어와 통신할 준비를 마친다."
- 위 문장은 `pygame.init()`의 기준 예시이며, 같은 밀도의 역할 설명을 모든 핵심코드 항목에 적용한다.

---

## 9) 검수 체크리스트

- [ ] `src/<slug>/` 아래 파일이 `INFO.md`, `CODELAB.md`, `hero.svg`로 구성되었는가
- [ ] `LAB_SLUGS`에 slug가 등록되어 허브 카드가 생성되는가
- [ ] `?lab=<slug>` 진입 시 상세가 정상 렌더링되는가
- [ ] 허브 카드에서 `hub_meta`가 출력되지 않는가
- [ ] 목차 하이라이트/코드 복사/하이라이팅이 정상 동작하는가
- [ ] 모바일에서 허브 1열, 상세 레이아웃이 깨지지 않는가
- [ ] `shared/`, `codelabs/`, 코드랩별 `index.html`이 다시 생기지 않았는가
